## Summary

<!-- What does this PR change and why? -->

Closes #

## Changes

-

## Testing

<!-- How was this verified? Paste relevant command output. -->

- [ ] `uv run ruff check src tests` passes
- [ ] `uv run ruff format --check src tests` passes
- [ ] `uv run pyright` passes
- [ ] `uv run pytest -m "not integration"` passes
- [ ] Integration tests run locally where relevant (`uv run pytest -m integration`)

## Checklist

- [ ] Scope is focused (one logical change)
- [ ] No secrets, credentials, or work-account references introduced
- [ ] Docs/plan updated if behavior changed
